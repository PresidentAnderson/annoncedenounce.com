{
  "name": "annoncedenounce-com",
  "version": "0.3.1",
  "private": true,
  "description": "Annonce Denonce static launch site and operating workflows.",
  "scripts": {
    "verify": "node scripts/verify-site.mjs",
    "version:bump": "node scripts/bump-version.mjs",
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
